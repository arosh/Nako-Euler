#「文章反転」命令は、「なでしこライブラリ〜」（http://www26.atwiki.jp/isoroku_be/）よりお借りしました。
●文章反転(Sを|Sの) 
	「nako_set("ret",string.reverse(nako_get("S")))」をLUA 
	retで戻る。

●回文判定(数の)
    長さに数の文字数を代入。
    
    左文字に数から(長さ / 2)文字左部分を代入。
    右文字に数から(長さ / 2)文字右部分を代入。
    右文字に右文字の文章反転を代入。
    
    もし、右文字と左文字で文字列比較が0ならば、
		返却値に真を代入。
    違えば
		返却値に偽を代入。
    返却値で戻る。

答えに0を代入。
iを999から100まで繰り返す
	jをiから100まで繰り返す
		もしi * j <= 答えならば
			抜ける。
		もし(i * j)の回文判定が真ならば、
			答えに(i * j)を代入。
答えを表示。
