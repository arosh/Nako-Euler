素数上限に1000000を代入。
素数番号に10001を代入。
素数表とは配列。

メモとは配列。
メモ[素数上限]に真を代入。
iを0から素数上限まで繰り返す
	メモ[i]に真を代入。

メモ[0]に偽を代入。
メモ[1]に偽を代入。
iを2から素数上限まで繰り返す
	もしメモ[i]が真ならば
		素数表にiを配列追加。
		もしi * i <= 素数上限ならば
			jとは整数。
			jを2から(素数上限 / i)の整数部分まで繰り返す
				メモ[i * j]に偽を代入。

素数表[素数番号 - 1]を表示。
