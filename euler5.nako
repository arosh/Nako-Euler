●最大公約数（AとBの）
	もしB > Aならば
		T = B。
		B = A。
		A = T。
		
	B != 0の間
		AにAをBで割った余りを代入。
		T = B。
		B = A。
		A = T。
	Aで戻る。

●最小公倍数（AとBの）
	A / (AとBの最大公約数) * Bで戻る。

上限に20を代入。
答えに1を代入。
iを1から上限まで繰り返す
	答えに答えとiの最小公倍数を代入。
答えを表示。
