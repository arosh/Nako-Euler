素数最大値に200万を代入。

素数表とは配列。
素数表[素数最大値]に真を代入。
iを0から素数最大値まで繰り返す
	素数表[i]に真を代入。

素数表[0]に偽を代入。
素数表[1]に偽を代入。
iを2から素数最大値まで繰り返す
	もし素数表[i]が真ならば
		答えにiを直接足す。
		もしi * i <= 素数最大値ならば
			jを2から(素数最大値 / i)の整数部分まで繰り返す
				素数表[i * j]に偽を代入。

答えを表示。
